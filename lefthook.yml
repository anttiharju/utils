output:
  - success
  - failure

pre-commit:
  jobs:
    - name: ShellCheck
      run: |
        for file in {staged_files}; do
          (cd "$(dirname "$file")" && shellcheck --color=always -x "$(basename "$file")")
        done
      glob: "{scripts/*,*.sh}"
      